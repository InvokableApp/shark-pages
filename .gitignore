_backups/
.DS_Store

# Flattened print PDFs: ~48MB of DERIVED files, regenerated by
# NUEVASHARK/_build/42-print-ready-pdfs.mjs in the factory repo.
# Not published here: this repo is served by GitHub Pages, so committing them
# would both bloat every clone and make the print masters publicly downloadable.
# That is a hosting decision, not a default.
nueva/_guides/print/
